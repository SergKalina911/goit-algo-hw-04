"""
                                        Третє завдання (не обов'язкове)

    Розробіть скрипт, який приймає шлях до директорії в якості аргументу командного рядка і візуалізує
структуру цієї директорії, виводячи імена всіх піддиректорій та файлів. Для кращого візуального 
сприйняття, імена директорій та файлів мають відрізнятися за кольором.

                        Вимоги до завдання:

    1. Створіть віртуальне оточення Python для ізоляції залежностей проєкту.
    2. Скрипт має отримувати шлях до директорії як аргумент при запуску. Цей шлях вказує, де знаходиться 
    директорія, структуру якої потрібно відобразити.
    3. Використання бібліотеки colorama для реалізації кольорового виведення.
    4. Скрипт має коректно відображати як імена директорій, так і імена файлів, використовуючи 
    рекурсивний спосіб обходу директорій (можна, за бажанням, використати не рекурсивний спосіб).
    5. Повинна бути перевірка та обробка помилок, наприклад, якщо вказаний шлях не існує або він не веде 
    до директорії.

                        Рекомендації для виконання:

    1. Спочатку встановіть бібліотеку colorama. Для цього створіть та активуйте віртуальне оточення Python, 
    а потім встановіть пакет за допомогою pip.
    2. Використовуйте модуль sys для отримання шляху до директорії як аргументу командного рядка.
    3. Для роботи з файловою системою використовуйте модуль pathlib.
    4. Забезпечте належне форматування виводу, використовуючи функції colorama.

                        Критерії оцінювання:

    1. Створення та використання віртуального оточення.
    2. Правильність отримання та обробки шляху до директорії.
    3. Точність виведення структури директорії.
    4. Коректне застосування кольорового виведення за допомогою colorama.
    5. Якість коду, включаючи читабельність, структурування та коментарі.

                        Приклад використання:

    Якщо виконати скрипт та передати йому абсолютний шлях до директорії як параметр.
        
        python hw03.py /шлях/до/вашої/директорії

    Це призведе до виведення в терміналі списку всіх піддиректорій та файлів у вказаній директорії з 
використанням різних кольорів для піддиректорій та файлів, що полегшить візуальне сприйняття файлової 
структури.

    Для директорії зі наступною структурою

📦picture
 ┣ 📂Logo
 ┃ ┣ 📜IBM+Logo.png
 ┃ ┣ 📜ibm.svg
 ┃ ┗ 📜logo-tm.png
 ┣ 📜bot-icon.png
 ┗ 📜mongodb.jpg

    Скрипт повинен вивести схожу  кольорову структуру"""


# Імпортуємо необхідні модулі
import sys
from pathlib import Path
from colorama import Fore, Style, init
# Ініціалізація colorama для підтримки кольорового виведення
init(autoreset=True)
"""Функція init() може приймати необов"язковий аргумент autoreset=True. Коли встановлено значення True,
Colorama автоматично скидає колір і стиль тексту до стандартних після кожного оператора print(), 
запобігаючи ненавмисному розтіканню кольорів у наступні рядки виводу."""

# Функція для рекурсивного виведення структури директорії
def print_directory_structure(path: Path, indent: str = ""):
    """Рекурсивно виводить структуру директорії з кольоровим форматуванням.
    Аргументи:
        path (Path): Шлях до директорії, яку потрібно вивести.
        indent (str): Відступ для візуального форматування.
    Повертає:
        None """
    
    # Перевірка, чи існує шлях і чи є він директорією
    if not path.exists():
        print(f"{Fore.RED}Помилка: {path} не існує.")
        return
    if not path.is_dir():
        print(f"{Fore.RED}Помилка: {path} не є директорією.")
        return

    for item in path.iterdir():
        # iterdir() повертає ітератор, який перебирає всі елементи в директорії path.
        # Цикл перебирає кожен елемент у директорії та виводить його з відповідним відступом.
        # Відображення імені директорії або файлу з кольоровим форматуванням
        
        if item.is_dir():
            # Якщо елемент є директорією, виводимо його з синім кольором
            # Використання Fore.BLUE для кольорового виведення імені директор
            print(f"{indent}{Fore.BLUE}{item.name}/")
            
            # Рекурсивний виклик для виведення вмісту піддиректорії з додатковим відступом
            # Додаємо два пробіли до відступу для візуального форматування
            # Це дозволяє створити ієрархічну структуру виводу
            print_directory_structure(item, indent + "  ")
        else:
            # Якщо елемент є файлом, виводимо його з зеленим кольором
            print(f"{indent}{Fore.GREEN}{item.name}")

if __name__ == "__main__":
    
    # Перевірка наявності аргументів командного рядка
    if len(sys.argv) != 2:
        print(f"{Fore.RED}Помилка: Будь ласка, вкажіть шлях до директорії.")
        # Вивести повідомлення про помилку та завершити програму
        sys.exit(1)
        """ За замовчуванням sys.exit() повертає код виходу 0, що означає успішне завершення, але 
            можно вказати інший код повернення, щоб повідомити про помилку або інше завершення."""
    
    # Отримання шляху до директорії з аргументів командного рядка. Використання pathlib для роботи з шляхами
    directory_path = Path(sys.argv[1])
    # Виклик функції виведення структури директорії
    print_directory_structure(directory_path)
    # Виведення повідомлення про успішне завершення
    print(f"{Fore.YELLOW}Структура директорії {directory_path} виведена успішно.")
    # Скидання стилю кольору
    print(Style.RESET_ALL)  
    # Завершення програми
    sys.exit(0)




